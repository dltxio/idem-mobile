// import config from "../../config.dev.json";
// import { put, post } from "../http";

// export const sendEmailVerificationEmail = async (
//   body: server.SendEmailVerificationEmailRequestBody,
// ) => put<server.SuccessResponse>(`${config.email.endpoint}`, body);

// export const verifyEmailCode = async (body: server.VerifyEmailRequestBody) =>
//   await post(`${config.email.endpoint}`, body);
